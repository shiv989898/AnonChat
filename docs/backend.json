{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user in the AnonChat application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "lastActive": {
          "type": "string",
          "description": "Timestamp of the user's last activity.",
          "format": "date-time"
        },
        "isAi": {
          "type": "boolean",
          "description": "Indicates if the user is an AI or human."
        }
      },
      "required": [
        "id"
      ]
    },
    "ChatMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChatMessage",
      "type": "object",
      "description": "Represents a single chat message between two users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChatMessage entity."
        },
        "senderId": {
          "type": "string",
          "description": "Reference to User who sent the message."
        },
        "content": {
          "type": "string",
          "description": "The text content of the chat message."
        },
        "timestamp": {
          "type": "string",
          "description": "Timestamp indicating when the message was sent.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "senderId",
        "content",
        "timestamp"
      ]
    },
    "ChatSession": {
      "$schema": "http://json-schema.org/draft-07-schema#",
      "title": "ChatSession",
      "type": "object",
      "description": "Represents a chat session between two users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ChatSession entity."
        },
        "user1Id": {
          "type": "string",
          "description": "Reference to the first user in the chat session."
        },
        "user2Id": {
          "type": "string",
          "description": "Reference to the second user in the chat session. Can be null if waiting for a partner."
        },
        "startTime": {
          "type": "string",
          "description": "Timestamp indicating when the chat session started.",
          "format": "date-time"
        },
        "endTime": {
          "type": "string",
          "description": "Timestamp indicating when the chat session ended. Can be null if the chat is active.",
          "format": "date-time"
        },
        "status": {
            "type": "string",
            "description": "The current status of the chat session.",
            "enum": ["waiting", "active", "guessing", "finished"]
        },
        "partnerType": {
            "type": "string",
            "description": "The type of partner in the chat session.",
            "enum": ["ai", "human"]
        },
        "messages": {
            "type": "array",
            "items": {
                "$ref": "#/entities/ChatMessage"
            }
        }
      },
      "required": [
        "id",
        "user1Id",
        "startTime",
        "status",
        "partnerType",
        "messages"
      ]
    },
    "WaitingPoolEntry": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "WaitingPoolEntry",
        "type": "object",
        "description": "An entry in the matchmaking waiting pool.",
        "properties": {
            "userId": {
                "type": "string"
            },
            "sessionId": {
                "type": "string"
            },
            "createdAt": {
                "type": "string",
                "format": "date-time"
            },
            "status": {
                "type": "string",
                "enum": ["waiting"]
            }
        },
        "required": ["userId", "sessionId", "createdAt", "status"]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "collections": [
      {
        "path": "/users/{userId}",
        "schema": "User",
        "description": "Stores user profiles."
      },
      {
        "path": "/chat_sessions/{chatSessionId}",
        "schema": "ChatSession",
        "description": "Stores chat sessions."
      },
      {
          "path": "/waiting_pool/{waitingPoolEntryId}",
          "schema": "WaitingPoolEntry",
          "description": "The matchmaking waiting pool."
      }
    ]
  }
}
